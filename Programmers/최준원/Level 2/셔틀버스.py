def solution(n, t, m, timetable):
    answer = ''
    # sorted로 시간 순 정렬 가능 (문자열이라). 시간 간의 비교도 가능하다.
    timetable = sorted(timetable)
    # 9시부터 n-1. 이후 t분마다 1씩 까인다.
    # 주어진 timetable에서 앞에서부터 출발시간까지 깐다.
    p_idx = 0

    # 콘의 탑승 시간
    while n > 1:
        # idx ++ ~m
        # if p_idx >
        pass

    # n이 1일 때, 탑승자 (timetable index)가 있다면 -1분으로 한다.
    # 탑승자가 없다면 출발 시간으로

    return answer

solution(2, 10, 2, ["09:10", "09:09", "08:00"])